# Portfolio — Reis

Site pessoal. React + Vite no frontend, Node puro no backend.

```bash
cd backend && npm run dev                                   # http://127.0.0.1:3000
cd frontend && npm install --ignore-scripts && npm run dev   # http://localhost:5173
```

Conteúdo do site em `frontend/src/content.js`.
